<UserControl x:Class="LaptopShop.WPF.Pages.Admin.OrderManagementTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="dgOrders"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionChanged="dgOrders_SelectionChanged"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Order ID" Binding="{Binding OrderId}" Width="80"/>
                <DataGridTextColumn Header="Customer" Binding="{Binding Customer.User.FullName}" Width="180"/>
                <DataGridTextColumn Header="Order Date" Binding="{Binding OrderDate}" Width="150"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount}" Width="120"/>
                <DataGridTextColumn Header="Shipment Status" Binding="{Binding Shipment.Status}" Width="130"/>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Grid.Row="1" Header="Order Detail" Padding="10">
            <DataGrid x:Name="dgOrderItems"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="OrderItem ID" Binding="{Binding OrderItemId}" Width="90"/>
                    <DataGridTextColumn Header="Product" Binding="{Binding SnapshotProductName}" Width="220"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding SnapshotPrice}" Width="120"/>
                    <DataGridTextColumn Header="Product ID" Binding="{Binding ProductId}" Width="100"/>
                    <DataGridTextColumn Header="ProductItem ID" Binding="{Binding ProductItemId}" Width="110"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>