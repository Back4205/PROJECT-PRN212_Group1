<UserControl x:Class="LaptopShop.WPF.Pages.Admin.UserManagementTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <DataGrid x:Name="dgUsers"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionChanged="dgUsers_SelectionChanged"
                  Margin="0,0,10,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="50"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="120"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="160"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10,0,0,0">
                <TextBlock Text="User Detail"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0,0,0,10"/>

                <TextBlock Text="User ID"/>
                <TextBox x:Name="txtUserId" Margin="0,0,0,8" IsReadOnly="True"/>

                <TextBlock Text="Username"/>
                <TextBox x:Name="txtUsername" Margin="0,0,0,8" IsReadOnly="True"/>

                <TextBlock Text="Full Name"/>
                <TextBox x:Name="txtFullName" Margin="0,0,0,8" IsReadOnly="True"/>

                <TextBlock Text="Email"/>
                <TextBox x:Name="txtEmail" Margin="0,0,0,8" IsReadOnly="True"/>

                <TextBlock Text="Roles" FontWeight="Bold" Margin="0,10,0,5"/>
                <ListBox x:Name="lbRoles"
                         Height="150"
                         SelectionMode="Multiple"
                         DisplayMemberPath="RoleName"/>

                <StackPanel Orientation="Horizontal" Margin="0,15,0,15">
                    <Button Content="Update Roles" Width="110" Margin="5" Click="btnUpdateRoles_Click"/>
                    <Button Content="Lock" Width="80" Margin="5" Click="btnLockUser_Click"/>
                    <Button Content="Unlock" Width="80" Margin="5" Click="btnUnlockUser_Click"/>
                </StackPanel>

                <Separator Margin="0,10,0,10"/>

                <TextBlock Text="Add New User"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0,0,0,10"/>

                <TextBlock Text="Username" FontWeight="Bold" Margin="0,5,0,3"/>
                <TextBox x:Name="txtNewUsername" Margin="0,0,0,8"/>

                <TextBlock Text="Password" FontWeight="Bold" Margin="0,5,0,3"/>
                <PasswordBox x:Name="txtNewPassword" Margin="0,0,0,8"/>

                <TextBlock Text="Full Name" FontWeight="Bold" Margin="0,5,0,3"/>
                <TextBox x:Name="txtNewFullName" Margin="0,0,0,8"/>

                <TextBlock Text="Email" FontWeight="Bold" Margin="0,5,0,3"/>
                <TextBox x:Name="txtNewEmail" Margin="0,0,0,8"/>

                <TextBlock Text="Phone" FontWeight="Bold" Margin="0,5,0,3"/>
                <TextBox x:Name="txtNewPhone" Margin="0,0,0,8"/>

                <TextBlock Text="Select Roles" FontWeight="Bold" Margin="0,8,0,5"/>
                <ListBox x:Name="lbNewUserRoles"
                         Height="120"
                         SelectionMode="Multiple"
                         DisplayMemberPath="RoleName"/>

                <Button Content="Add User"
                        Width="120"
                        Margin="0,10,0,0"
                        Click="btnAddUser_Click"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>