<UserControl x:Class="LaptopShop.WPF.Pages.Admin.ProductManagementTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,10,0">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox x:Name="txtSearchProduct" Width="250" Margin="0,0,10,0"/>
                <Button Content="Search" Width="80" Click="btnSearchProduct_Click"/>
                <Button Content="Load All" Width="80" Margin="10,0,0,0" Click="btnLoadProducts_Click"/>
            </StackPanel>

            <DataGrid x:Name="dgProducts" AutoGenerateColumns="False" IsReadOnly="True" 
                      Height="550" SelectionChanged="dgProducts_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding ProductId}" Width="50"/>
                    <DataGridTextColumn Header="Code" Binding="{Binding ProductCode}" Width="100"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding ProductName}" Width="200"/>
                    <DataGridTextColumn Header="Brand" Binding="{Binding Brand}" Width="100"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding BasePrice}" Width="100"/>
                    <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="60"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel Grid.Column="1">
            <TextBlock Text="Product Details" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

            <TextBlock Text="Product ID"/>
            <TextBox x:Name="txtProductId" IsReadOnly="True" Margin="0,0,0,10"/>

            <TextBlock Text="Product Code"/>
            <TextBox x:Name="txtProductCode" Margin="0,0,0,10"/>

            <TextBlock Text="Product Name"/>
            <TextBox x:Name="txtProductName" Margin="0,0,0,10"/>

            <TextBlock Text="Brand"/>
            <TextBox x:Name="txtBrand" Margin="0,0,0,10"/>

            <TextBlock Text="Base Price"/>
            <TextBox x:Name="txtBasePrice" Margin="0,0,0,10"/>

            <TextBlock Text="Image URL"/>
            <TextBox x:Name="txtImgUrl" Margin="0,0,0,10"/>

            <CheckBox x:Name="chkIsActive" Content="Is Active" Margin="0,0,0,20"/>

            <UniformGrid Columns="2">
                <Button Content="Add" Margin="5" Click="btnAddProduct_Click"/>
                <Button Content="Update" Margin="5" Click="btnUpdateProduct_Click"/>
                <Button Content="Delete" Margin="5" Click="btnDeleteProduct_Click"/>
                <Button Content="Clear" Margin="5" Click="btnClearProductForm_Click"/>
            </UniformGrid>
        </StackPanel>
    </Grid>
</UserControl>